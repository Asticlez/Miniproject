generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native"] // Optional for binary compatibility
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id       Int    @id @default(autoincrement())
  email    String @unique
  password String
  name     String
}